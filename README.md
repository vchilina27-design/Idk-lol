# Zip Packer

A simple Python script to pack files and directories into a .zip archive.

## Usage

```bash
python3 zip_packer.py <source_path> <output_filename>
```

Example:
```bash
python3 zip_packer.py ./my_folder my_archive.zip
```
